# Storage Case (WIP)

Not built yet. This will be the parametric OpenSCAD storage case template — tray-insert style, slots sized to each role's base diameter and height, taking the same rig parameters as input so a new matchup's case is a re-render, not a new design.

See [`docs/PROJECT-SPEC.md`](../docs/PROJECT-SPEC.md#storage-case-system) for the full design.

Licensed under [MIT](../LICENSE).
